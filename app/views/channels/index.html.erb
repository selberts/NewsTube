<%= render 'form' %>

<h4 style="background:#444;">Local</h4>
<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Zipcode</th>
      <th>Channelid</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @channels.each do |channel| 
      if channel.category == 'local' %>
      <tr>
        <td><%= channel.name %></td>
        <td><%= channel.zipcode %></td>
        <td><%= channel.channelid %></td>
        <td><%= link_to 'Show', channel %></td>
        <td><%= link_to 'Edit', edit_channel_path(channel) %></td>
        <td><%= link_to 'Destroy', channel, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end
    end %>
  </tbody>
</table>
<br/>

<h4 style="background:#444;">Advocacy</h4>
<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Channelid</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @channels.each do |channel| 
      if channel.category == 'advocacy' %>
      <tr>
        <td><%= channel.name %></td>
        <td><%= channel.channelid %></td>
        <td><%= link_to 'Show', channel %></td>
        <td><%= link_to 'Edit', edit_channel_path(channel) %></td>
        <td><%= link_to 'Destroy', channel, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end
    end %>
  </tbody>
</table>
<br/>

<h4 style="background:#444;">Prominent</h4>
<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Channelid</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @channels.each do |channel| 
      if channel.category == 'prominent' %>
      <tr>
        <td><%= channel.name %></td>
        <td><%= channel.channelid %></td>
        <td><%= link_to 'Show', channel %></td>
        <td><%= link_to 'Edit', edit_channel_path(channel) %></td>
        <td><%= link_to 'Destroy', channel, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end
    end %>
  </tbody>
</table>

<br/><br/><br/>

<%= form_for :import_csv, :url => {:action => 'loadchanneldb'}, :html => {:multipart => true} do |f| %>
  <%= f.file_field :csv %>
  <%= f.submit 'Upload CSV', :class => 'btn' %>
<%end%>

<%= link_to 'Download DB', channels_path(format: "csv") %>
<br/><br/>
<%= link_to "Delete DB", { :action => "resetchanneldb"}, :confirm => 'Are you sure?'%>
