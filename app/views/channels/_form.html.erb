<%= form_for(@channel) do |f| %>
  <% if @channel.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@channel.errors.count, "error") %> prohibited this channel from being saved:</h2>

      <ul>
      <% @channel.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <h4>Name:</h4>
    <input id="channel_name" name="channel[name]" type="text" />
  </div>
  <div class="field">
    <h4>Category:</h4>
    <select id="channel_category" name="channel[category]">
      <option value="local">Local</option>
      <option value="advocacy">Advocacy</option>
      <option value="prominent">Prominent</option>
    </select>
  </div>
  <div class="field">
    <h4>ZipCode:</h4>
    <input id="channel_zipcode" name="channel[zipcode]" type="text" />
  </div>
  <div>
    <input class='btn' style="margin-top:20px;" name="commit" type="submit" value="Create" />
  </div>
<% end %>
